import { Props, ScriptProps } from 'next/script'
import React from 'react'

interface ScriptProps {
    title: string;
    image: string;
}

const EventCard = ({title, image}: ScriptProps) => {
  return (
    
  )
}

export default EventCard